describe('add prodcut to cart', () => {
  it('should be able to navigate to the product page and add it to the car', () => {
    cy.visit('http://localhost:3000')
    // cy.get('a[hrefˆ="/product"]').first().click()
  })
})
